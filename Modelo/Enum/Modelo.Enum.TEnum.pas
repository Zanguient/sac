unit Modelo.Enum.TEnum;

interface

  type
    EnumStatusBanco = (BLOQUEADO, LIBERADO);
    EnumStatusConta = (PAGO, PENDENTE);
    EnumTipoContaCaixa = (PAGAS, RECEBIDAS, PAGASBANCO, RECEBIDASBANCO);
    //EnumDescricaoContaEstornada = (ESTORNO='ESTORNO DE VALOR');


implementation

end.
